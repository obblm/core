<header class="ui fixed inverted main menu">
    <nav class="ui container">
        <a class="launch icon item">
            <i class="content icon"></i>
        </a>
        <div class="item">
            <h1>{{ 'obblm.layout.header.title'|trans }}</h1>
        </div>
        <div class="right menu">
        {% if(is_granted('ROLE_OBBLM_COACH')) %}
            <a class="icon item" href="{{ path('obblm.logout')}}" title="{{ 'obblm.layout.header.logout'|trans }}">
                <i aria-hidden="true" class="icon log out"></i>
            </a>
        {% endif %}
        </div>
    </nav>
</header>
